---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  name: istiod
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
  project: homelab-platform
  revisionHistoryLimit: 10
  source:
    directory:
      include: '{*.yaml,*.yml}'
      recurse: true
    path: artifacts/infrastructure/istiod
    repoURL: https://github.com/nandiheath/homelab-public.git
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
      limit: 5
    syncOptions:
      - Validate=false
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - RespectIgnoreDifferences=true
      - ServerSideApply=true
